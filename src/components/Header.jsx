import React from "react";
import Nav from "./Nav";

// the header
export default function Header(){
  return(
    <header>
      <div>
        {/* <h1>
          This is the Header.
        </h1> */}
      </div>

      <Nav />
    </header>
  )
}